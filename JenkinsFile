pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Test Stage'
                sh ls build/index.html
                sh echo 'File Exist'
                sh 'npm test'
            }
        }
    }
}