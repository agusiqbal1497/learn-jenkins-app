pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                        echo 'Test Stage'
                        sh stat build/index.html && echo 'File Exist'
            }
        }
    }
}