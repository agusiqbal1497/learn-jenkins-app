pipeline {
    agent any

    stages {
        stage('Test') {
            agent {
                docker {
                    echo "Test Stage"
                    reuseNode true
                }
            }
        }
    }
}