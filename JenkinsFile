pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Test Stage'
                sh ls -la
                sh stat build/index.html && echo 'File Exist'
            }
        }
    }
}